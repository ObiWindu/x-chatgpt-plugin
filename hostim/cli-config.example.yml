# Example ~/.config/hostim/config.yml
# Do NOT commit a real token.
#
# Hostim CLI resolves values as:
# flag -> environment variable -> config file -> built-in default
#
# Prefer HOSTIM_TOKEN in CI instead of storing it in a file.

apiUrl: https://api.hostim.dev
currentProject: YOUR_PROJECT
# token: YOUR_TOKEN
