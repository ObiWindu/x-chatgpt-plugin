services:
  x-publisher:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PORT: "3000"
      DATABASE_PATH: /app/data/app.db
    ports:
      - "3000"
    volumes:
      - x-publisher-data:/app/data
    restart: unless-stopped

volumes:
  x-publisher-data:
